# BuildOrbit — Environment Variables
#
# IMPORTANT: This app runs on Polsia infrastructure. Most variables below are
# provisioned automatically by Polsia on Render. You do NOT need to set them
# manually in production — they are injected at deploy time.
#
# When migrating OFF Polsia, you will need to provision these yourself.
# See the "Polsia-provisioned" annotations below.
#
# ── AI / LLM ─────────────────────────────────────────────────────────────────
#
# [Polsia-provisioned] Polsia's OpenAI proxy key — routes requests through
# Polsia's AI infrastructure. When migrating off Polsia, replace with a
# direct OpenAI API key from platform.openai.com.
OPENAI_API_KEY=

# [Polsia-provisioned] Polsia's OpenAI-compatible proxy base URL.
# When migrating off Polsia, remove this line (or set to https://api.openai.com/v1).
OPENAI_BASE_URL=https://polsia.com/ai/openai/v1

# [Polsia-provisioned] Polsia API key — used as fallback for email proxy and
# JWT secret derivation. Same value as OPENAI_API_KEY in Polsia's setup.
POLSIA_API_KEY=

# [Polsia-provisioned] Alternative Polsia token alias (same as POLSIA_API_KEY).
# Kept for compatibility; prefer POLSIA_API_KEY when migrating off Polsia.
POLSIA_API_TOKEN=

# ── Database ─────────────────────────────────────────────────────────────────
#
# [Polsia-provisioned] Neon PostgreSQL connection string.
# When migrating off Polsia, provision a new Neon database at neon.tech
# or any PostgreSQL provider and set this to your connection string.
DATABASE_URL=

# ── Server ───────────────────────────────────────────────────────────────────
#
# Application base URL — used for email links, Stripe redirects, and CLI endpoints.
# Defaults to https://buildorbit.polsia.app if not set.
APP_URL=https://buildorbit.polsia.app

# Server port — defaults to 3000. Render sets this automatically.
PORT=3000

# Node environment — "production" on Render, "development" locally.
NODE_ENV=development

# ── Authentication ────────────────────────────────────────────────────────────
#
# JWT signing secret. If not set, derived from POLSIA_API_KEY (secure fallback).
# Set explicitly in production for stronger isolation.
JWT_SECRET=

# ── Email ─────────────────────────────────────────────────────────────────────
#
# All transactional emails (magic links, welcome, password reset) go through the
# Polsia email proxy (https://polsia.com/api/proxy/email/send).
# POLSIA_API_KEY is auto-provisioned on Render — no manual setup needed.

# ── Billing ───────────────────────────────────────────────────────────────────
#
# Stripe secret key — required for subscription management and Customer Portal.
STRIPE_SECRET_KEY=

# Stripe webhook signing secret — verifies webhook events from Stripe.
# If not set, webhook signature verification is skipped (dev mode only).
STRIPE_WEBHOOK_SECRET=

# ── GitHub OAuth ─────────────────────────────────────────────────────────────
#
# Required for the GitHub integration (Connect GitHub button in /github settings).
# Create a GitHub OAuth App at https://github.com/settings/developers.
# Set the Authorization callback URL to: https://buildorbit.polsia.app/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ── Access Control ────────────────────────────────────────────────────────────
#
# Comma-separated list of user IDs that get admin privileges (bootstrapping only).
# Example: ADMIN_USER_IDS=1,2,5
ADMIN_USER_IDS=

# ── Development Only ──────────────────────────────────────────────────────────
#
# Enable mock mode — bypasses auth and mocks Postmark. NEVER set in production
# (server.js enforces this: process throws if NODE_ENV=production and MOCK_MODE=true).
MOCK_MODE=false
